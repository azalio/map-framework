# MAP Framework — Development Notes

This repository’s canonical agent instructions live in `CLAUDE.md` at the repo root.

- Read: `../CLAUDE.md`
- Reminder: `.claude/` is the development working set; `src/mapify_cli/templates/` is what ships to users via `mapify init`.

